# common
opentracing:
  datadog:
    enabled: true
    trace:
      enabled: false
      health-metrics:
        enabled: true
      startup-logs: true
    logs-injection: true

logging:
  level:
    io:
      opentelemetry: WARN
    datadog:
      trace:
        common:
          writer:
            LoggingWriter: WARN
---

# dev profile
spring:
  config:
    activate:
      on-profile: local

---

# stage profile
spring:
  config:
    activate:
      on-profile: stage

---

# production profile
spring:
  config:
    activate:
      on-profile: production
