# common
spring:
  jersey:
    application-path: /internal/v1

camunda:
  bpm:
    authorization:
      enabled: false
      tenant-check-enabled: false
    database:
      schema-update: false
    deployment-resource-pattern: classpath*:com/depop/cx/drc/workflow/*.bpmn
    default-serialization-format: application/json
    generic-properties:
      properties:
        historyCleanupStrategy: removalTimeBased
        historyRemovalTimeStrategy: start

---

# h2 profile
spring:
  config:
    activate:
      on-profile: h2
camunda:
  bpm:
    admin-user:
      id: demo
      password: demo
    database:
      schema-update: true

---

# local profile
spring:
  config:
    activate:
      on-profile: local

---

# stage profile
spring:
  config:
    activate:
      on-profile: stage
camunda:
  bpm:
    generic-properties:
      properties:
        historyCleanupEnabled: true
        historyTimeToLive: P30D
        historyCleanupJobLogTimeToLive: P7D
        historyCleanupBatchWindowStartTime: "07:00"
        historyCleanupBatchWindowEndTime: "20:00"

---

# production profile
spring:
  config:
    activate:
      on-profile: production
camunda:
  bpm:
    generic-properties:
      properties:
        historyCleanupEnabled: true
        historyTimeToLive: P180D
        historyCleanupJobLogTimeToLive: P7D
        historyCleanupBatchWindowStartTime: "05:00"
        historyCleanupBatchWindowEndTime: "11:00"
